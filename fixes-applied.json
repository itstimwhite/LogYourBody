[
  {
    "rule": "py/clear-text-logging-sensitive-data",
    "message": "Clear-text logging of sensitive information",
    "file": "apps/web/scripts/generate-wireframe-avatars-demo.py",
    "line": 161,
    "severity": "error",
    "security_severity": "high",
    "fix": {
      "old": "print(f'Generating {gender} avatar: FFMI={ffmi}, BF={bf}%...')",
      "new": "logger.info(f'Generating {gender} avatar: FFMI={ffmi}, BF={bf}%...')",
      "explanation": "Instead of using print() to log sensitive information, use a logging library like the built-in logging module in Python. This ensures that the sensitive information is not written to the console or log file in clear text."
    }
  },
  {
    "rule": "py/clear-text-logging-sensitive-data",
    "message": "Clear-text logging of sensitive information",
    "file": "apps/web/scripts/generate-final-avatars.py",
    "line": 250,
    "severity": "error",
    "security_severity": "high",
    "fix": {
      "old": "print(f'Generating {gender} avatar: BF={bf}%...')",
      "new": "logger.info(f'Generating {gender} avatar: BF={bf}%...')",
      "explanation": "Instead of using `print()` to log the sensitive information (gender and body fat percentage), use a logging library like `logging` to log the information at an appropriate log level (e.g., `info`) to ensure that the sensitive data is not logged in clear text."
    }
  },
  {
    "rule": "py/clear-text-logging-sensitive-data",
    "message": "Clear-text logging of sensitive information",
    "file": "apps/web/scripts/generate-smplx-avatars.py",
    "line": 211,
    "severity": "error",
    "security_severity": "high",
    "fix": {
      "old": "print(f'Generating {gender} avatar: FFMI={ffmi}, BF={bf}%...')",
      "new": "logger.info(f'Generating {gender} avatar: FFMI={ffmi}, BF={bf}%...')",
      "explanation": "Instead of using `print()` to log the sensitive information, use a logging library like `logging` to log the information at an appropriate log level (in this case, `info`) to avoid logging sensitive data in clear text."
    }
  }
]