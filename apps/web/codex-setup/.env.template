# LogYourBody Environment Variables Template
# Copy this file to .env.local and fill in your values

# ============================================
# REQUIRED - Core Application Settings
# ============================================

# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Application URL (Required)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# ============================================
# OPTIONAL - Feature-Specific Settings
# ============================================

# OpenAI Configuration (Required for PDF parsing)
OPENAI_API_KEY=sk-proj-your-openai-api-key

# Twilio Configuration (Required for SMS authentication)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_VERIFY_SERVICE_SID=your-verify-service-sid

# RevenueCat Configuration (Required for subscriptions)
NEXT_PUBLIC_REVENUECAT_API_KEY_APPLE=your-apple-api-key
NEXT_PUBLIC_REVENUECAT_API_KEY_GOOGLE=your-google-api-key
REVENUECAT_WEBHOOK_SECRET=your-webhook-secret

# ============================================
# DEPLOYMENT - Vercel Settings
# ============================================

# Vercel Configuration (Required for deployment)
VERCEL_TOKEN=your-vercel-token
VERCEL_ORG_ID=your-vercel-org-id
VERCEL_PROJECT_ID=your-vercel-project-id

# ============================================
# DEVELOPMENT - Local Settings
# ============================================

# Database Password (for migrations)
SUPABASE_DB_PASSWORD=your-database-password

# ============================================
# NOTES
# ============================================
# 1. Never commit this file with real values
# 2. Add .env.local to .gitignore
# 3. For production, set these in Vercel dashboard
# 4. OpenAI API key is only needed for PDF import feature
# 5. Twilio is only needed for SMS authentication
# 6. RevenueCat is only needed for paid subscriptions